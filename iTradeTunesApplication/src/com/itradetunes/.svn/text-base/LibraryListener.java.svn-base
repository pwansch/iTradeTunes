package com.itradetunes;

import java.util.*;

public interface LibraryListener extends EventListener {
    // Called when the library gets changed
    public void libraryChanged(LibraryChangeEvent lce);
}